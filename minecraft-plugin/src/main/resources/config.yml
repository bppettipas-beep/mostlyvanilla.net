# URL of the Discord bot's HTTP API server
bot-api-url: "http://localhost:3000"

# Must match API_SECRET in the Discord bot's .env
api-secret: "change-me-to-something-random"

# Remind unlinked players to run /discord on join
remind-on-join: true

messages:
  prefix: "&a[MostlyVanilla]&r "
